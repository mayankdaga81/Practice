### **Ready for the Day 5 Revision?**

Since we've reached the **25% milestone (Day 5 of 20)**, let's do a quick "Rapid Fire" review of what we've covered so far. This will help cement the concepts before we move into Day 6.

**Day 1-5 Summary:**

- **Day 1:** Props, State, and Component Architecture.
- **Day 2:** Side Effects (`useEffect`) and Memory Leaks (Cleanup).
- **Day 3:** Data Fetching, Loading/Error states, and `AbortController`.
- **Day 4:** Logic Abstraction with **Custom Hooks**.
- **Day 5:** Global State with **Context API**.

**Revision Question 1:** When using `useEffect`, why is it dangerous to update a state variable that is also listed in the dependency array?

**Revision Question 2:** In your `useFetch` hook, what would happen if you forgot to check `if (error.name === "AbortError")` in the catch block?

**Revision Question 3:** Why do we wrap the `value` object in `useMemo` inside a Context Provider?

**Answer these, and we will officially close the "Basics & API" chapter!**
